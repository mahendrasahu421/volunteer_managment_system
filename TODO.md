# TODO
- [x] Fix `update_certificate_data()` to preserve CKEditor HTML (remove `strip_tags` + whitespace collapse).
- [ ] Ensure `certificate_email` column can store HTML unchanged.
- [ ] Quick manual test: Edit And Update → type multiple lines → Save → Preview/Send → verify formatting.


